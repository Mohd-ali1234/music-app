// Media Session is a browser API. Native lock-screen integration is handled by
// expo-audio, so this component intentionally does nothing on native platforms.
export function MediaSession() {
  return null;
}
