# Backend runtime dependencies (curated).
#
# The previous requirements.txt was an unfiltered global `pip freeze` (~280
# packages: torch, streamlit, langchain, ...) that the backend never imported.
# This file lists only what the service actually uses. The old file remains in
# git history if a transitive pin ever needs to be recovered.

# --- Web framework / server ---
fastapi==0.135.1
uvicorn[standard]==0.41.0

# --- Validation / config ---
pydantic==2.12.5
email-validator==2.3.0        # required by pydantic EmailStr
python-dotenv==1.2.2

# --- Database ---
pymongo==4.16.0

# --- Auth ---
bcrypt==5.0.0
PyJWT==2.13.0

# --- Local secret storage (desktop app: Gemini API key via OS credential store) ---
keyring==25.6.0

# --- Search ranking / caching ---
rapidfuzz>=3.9,<4
cachetools==5.5.2

# --- External providers (YouTube / YouTube Music) ---
yt-dlp==2026.6.9
ytmusicapi==1.12.1
# PO-token provider plugin: yt-dlp auto-discovers this on import and calls
# out to a bgutil-ytdlp-pot-provider HTTP server (see YTDLP_POT_PROVIDER_URL
# in .env.example) to bypass YouTube's bot-check without needing cookies.
bgutil-ytdlp-pot-provider==1.3.1
